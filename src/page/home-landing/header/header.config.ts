import { RouteNames } from "@/router/route-config";

const headerConfig = {
  navSequence: [
    RouteNames.home,
    RouteNames.services,
    RouteNames.industries,
    RouteNames.portfolio,
    RouteNames.pricing,
    RouteNames.about,
    RouteNames.contact,
  ],
};

export default headerConfig;
